import 'package:flutter/material.dart';

class ResponsablePage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Responsable'),),
      body: Center(
        child: Text('Responsable'),
      ),

    );
  }
}
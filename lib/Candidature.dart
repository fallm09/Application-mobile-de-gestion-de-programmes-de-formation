import 'package:flutter/material.dart';

class CanditaturePage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Candidature'),),
      body: Center(
        child: Text('Candidature'),
      ),

    );
  }
}